{
  "name": "mealplanner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf ./public && mkdir -p ./public",
    "test": "npx ava build/**/*.spec.js",
    "test:watch": "npx ava --watch build/**/*.spec.js",
    "start": "npm run build",
    "tsc:watch": "npx tsc -w -p ts.tsconfig.json; npx tsc -w -p js.tsconfig.json",
    "html:watch": "npx chokidar \"./src/**/*.html.js\" -c \"node ./scripts/build-html.js\"",
    "static:watch": "npx chokidar \"./src/**/*.(js|css|svg)\" -c \"npm run build:static\"",
    "rollup:watch": "npx rollup -c -w",
    "watch": "npm-run-all -s build -p tsc:watch html:watch static:watch test:watch",
    "serve": "http-server -p 5500 -c-1 -g ./public",
    "tsc": "npx tsc -p ts.tsconfig.json; npx tsc -p js.tsconfig.json",
    "rollup": "npx rollup -c",
    "build:html": "node ./scripts/build-html.js",
    "build:static": "cd ./src; find . ! '(' -name '*.html.js' -o -path '*/layouts/*' ')' -name '*.js' -o -name '*.css' -o -name '*.svg' | cpio -pdm ../public; cd ..;",
    "build:service-worker": "node ./scripts/build-sources.js",
    "build": "npm-run-all -s build:service-worker clean tsc build:html build:static",
    "build:external:rollup": "npx rollup -c",
    "build:external:type:template": "npx tsc -d -m ES2020 --outDir ./src/app/utils/ ./external/template.ts 2> /dev/null -p",
    "build:external": "npm-run-all build:external:rollup build:external:type:template"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.643.0",
    "fp-ts-contrib": "^0.1.15",
    "glob": "^7.1.6",
    "idb": "^5.0.1",
    "stage0": "0.0.25"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^11.0.2",
    "@rollup/plugin-node-resolve": "^7.1.1",
    "@rollup/plugin-sucrase": "^3.0.1",
    "@rollup/plugin-typescript": "^3.1.1",
    "@types/debounce": "^1.2.0",
    "@types/node": "^12.12.30",
    "@types/service_worker_api": "0.0.9",
    "ava": "^3.5.0",
    "chokidar-cli": "^2.1.0",
    "http-server": "^0.12.1",
    "netlify-lambda": "^1.6.3",
    "nodemon": "^2.0.2",
    "npm-run-all": "^4.1.5",
    "rollup": "^1.32.1",
    "typescript": "^3.8.3"
  }
}
