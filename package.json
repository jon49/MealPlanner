{
  "type": "module",
  "name": "mealplanner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./build & rm -rf ./public && mkdir -p ./build & mkdir -p ./public",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:node": "nodemon ./dist/",
    "start": "echo doh!",
    "start:lambda": "netlify-lambda serve functions",
    "build:lambda": "netlify-lambda build functions",
    "tsc:watch": "npx tsc -w",
    "html:watch": "npx chokidar \"./build/**/*.html.js\" -c \"node --experimental-modules ./scripts/build-html.js {path}\"",
    "rollup:watch": "npx rollup -c -w",
    "watch": "npm-run-all -s clean -p tsc:watch rollup:watch html:watch",
    "serve": "http-server -p 8080 -c-1 -g ./public",
    "tsc": "npx tsc",
    "rollup": "npx rollup -c",
    "build:html": "node --experimental-modules ./scripts/build-html.js",
    "build": "npm-run-all -s clean tsc rollup build:html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.610.0",
    "debounce": "^1.2.0",
    "glob": "^7.1.6",
    "idb": "^5.0.1",
    "stage0": "0.0.25"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^11.0.2",
    "@rollup/plugin-node-resolve": "^7.1.1",
    "@rollup/plugin-typescript": "^3.0.0",
    "@types/debounce": "^1.2.0",
    "@types/node": "^12.12.24",
    "chokidar-cli": "^2.1.0",
    "http-server": "^0.12.1",
    "netlify-lambda": "^1.6.3",
    "nodemon": "^2.0.2",
    "npm-run-all": "^4.1.5",
    "rollup": "^1.31.0",
    "typescript": "^3.7.5"
  }
}
